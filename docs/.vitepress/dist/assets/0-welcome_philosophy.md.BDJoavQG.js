import{_ as a,o as i,c as t,a2 as r}from"./chunks/framework.BcTx5ZBM.js";const m=JSON.parse('{"title":"The Architecture Philosophy","description":"","frontmatter":{"next":{"text":"1.1 What is a Game Engine?","link":"/1-foundations/01-what-is-a-game-engine"}},"headers":[],"relativePath":"0-welcome/philosophy.md","filePath":"0-welcome/philosophy.md","lastUpdated":null}'),o={name:"0-welcome/philosophy.md"};function n(s,e,c,l,p,h){return i(),t("div",null,[...e[0]||(e[0]=[r('<h1 id="the-architecture-philosophy" tabindex="-1">The Architecture Philosophy <a class="header-anchor" href="#the-architecture-philosophy" aria-label="Permalink to &quot;The Architecture Philosophy&quot;">​</a></h1><p>Before we study any specific file or pattern, we need to understand the principles that guided every architecture decision in jygame.</p><p>These principles are not unique to jygame. They appear in game engines, rendering frameworks, and performance-critical systems across the industry. Understanding them first makes the code you will read later feel inevitable rather than arbitrary.</p><h2 id="principle-1-ownership" tabindex="-1">Principle 1: Ownership <a class="header-anchor" href="#principle-1-ownership" aria-label="Permalink to &quot;Principle 1: Ownership&quot;">​</a></h2><p>Every piece of data has exactly one owner.</p><p>In jygame, a <code>Sprite</code> does not own its rendering logic. A <code>Group</code> does not own its collision logic. The <code>RenderSystem</code> owns rendering. The <code>CollisionSystem</code> owns collision.</p><p>This seems obvious, but many beginner codebases mix ownership: a sprite has an <code>update()</code> method that moves itself and a <code>render()</code> method that draws itself. These responsibilities look like they belong together, but they create problems:</p><ul><li>Changing how movement works requires changing every sprite</li><li>Adding a new visual effect requires changing every sprite</li><li>Testing movement requires creating a sprite with rendering resources</li></ul><p>When ownership is separated, each concern can change independently.</p><h2 id="principle-2-zero-allocation-hot-path" tabindex="-1">Principle 2: Zero-Allocation Hot Path <a class="header-anchor" href="#principle-2-zero-allocation-hot-path" aria-label="Permalink to &quot;Principle 2: Zero-Allocation Hot Path&quot;">​</a></h2><p>Games run at 60 frames per second. That means 60 opportunities per second for the garbage collector to pause your game.</p><p>The garbage collector runs when memory is allocated and released. If your game allocates memory every frame — creating temporary arrays, objects, or strings — the garbage collector will eventually run. When it runs during gameplay, the player sees a stutter.</p><p>jygame&#39;s hot path — the code that runs every frame for every particle — aims for <strong>zero allocations</strong>. No new arrays, no new objects, no new closures on the hot path. Memory is pre-allocated in pools. Data structures are reused. Output arrays are passed in by the caller (<code>out</code> parameters) rather than created fresh.</p><p>This is not micro-optimization. At scale — thousands of particles, hundreds of entities — allocation pressure is the difference between 60 FPS and 30 FPS.</p><h2 id="principle-3-data-oriented-design" tabindex="-1">Principle 3: Data-Oriented Design <a class="header-anchor" href="#principle-3-data-oriented-design" aria-label="Permalink to &quot;Principle 3: Data-Oriented Design&quot;">​</a></h2><p>Object-oriented programming organizes code around objects. A <code>Particle</code> object has <code>x</code>, <code>y</code>, <code>vx</code>, <code>vy</code>, <code>life</code>, and methods that operate on them.</p><p>Data-oriented design asks a different question: what operations do we perform, and what data layout makes those operations fastest?</p><p>When a particle system updates 10,000 particles, it must:</p><ol><li>Apply forces to each particle&#39;s velocity</li><li>Update each particle&#39;s position</li><li>Decrease each particle&#39;s remaining life</li><li>Check if each particle has died</li></ol><p>These operations access specific fields: velocity, position, life. In an object-oriented layout, each particle&#39;s fields are scattered across memory. Iterating 10,000 particles means jumping between 10,000 memory locations.</p><p>In a data-oriented layout, all velocities are in one contiguous array, all positions in another, all lives in a third. The CPU reads them sequentially — and sequential memory access is dramatically faster than random access.</p><p>jygame&#39;s <code>SoAParticleStorage</code> implements exactly this pattern.</p><h2 id="principle-4-composition-over-inheritance" tabindex="-1">Principle 4: Composition Over Inheritance <a class="header-anchor" href="#principle-4-composition-over-inheritance" aria-label="Permalink to &quot;Principle 4: Composition Over Inheritance&quot;">​</a></h2><p>Inheritance says: a fire particle IS a particle. A smoke particle IS a particle.</p><p>Composition says: a fire particle has a fade behavior, a velocity behavior, and a color behavior.</p><p>The difference matters because real particle behaviors do not fit cleanly into inheritance hierarchies. A fire particle fades, moves, and changes color. A smoke particle also fades, moves, and changes color — but differently. An explosion particle fades, moves, changes color, and spawns child particles.</p><p>With inheritance, you quickly end up with a deep hierarchy: <code>MovingParticle &gt; FadingParticle &gt; ColoredParticle &gt; SpawningParticle &gt; FireParticle</code>. Adding one new behavior multiplies the hierarchy.</p><p>With composition, you assemble behaviors like building blocks. A particle is born with a set of <em>modifiers</em> — independent objects that each implement a small behavior. The modifier stack processes each particle through every modifier in order.</p><p>jygame&#39;s entire modifier system — 15 modifier types plus a modifier stack — is built on this principle.</p><h2 id="principle-5-progressive-complexity" tabindex="-1">Principle 5: Progressive Complexity <a class="header-anchor" href="#principle-5-progressive-complexity" aria-label="Permalink to &quot;Principle 5: Progressive Complexity&quot;">​</a></h2><p>jygame&#39;s particle system supports three backends: CPU, GPU operator, and GPU compute.</p><p>The CPU backend is the simplest. It simulates particles in JavaScript, builds a render command buffer, and hands it to a renderer. It handles 5,000-10,000 particles per frame on modern hardware.</p><p>The GPU operator mode keeps CPU simulation but renders with WebGL2 instancing. This offloads the rendering bottleneck to the GPU.</p><p>The GPU compute mode runs the entire simulation on the GPU using WebGPU compute shaders. It handles 100,000+ particles per frame.</p><p>Each mode is more complex than the previous. Each mode solves a scaling problem that the previous mode hits. This progressive complexity means you never pay for capability you do not need. A simple particle effect does not require a WebGPU compute pipeline.</p><h2 id="principle-6-pluggable-abstractions" tabindex="-1">Principle 6: Pluggable Abstractions <a class="header-anchor" href="#principle-6-pluggable-abstractions" aria-label="Permalink to &quot;Principle 6: Pluggable Abstractions&quot;">​</a></h2><p>When jygame needs to choose between implementations — CPU vs GPU simulation, Canvas vs WebGL vs WebGPU rendering, HTML Audio vs Web Audio — it does not hard-code the choice. It defines an abstract interface and lets the caller select the implementation.</p><p>The <code>ParticleRenderer</code> base class defines <code>render()</code>. <code>CanvasParticleRenderer</code> and <code>GpuParticleRenderer</code> each implement it differently. The particle system does not know which renderer it has. It just calls <code>render()</code>.</p><p>This is the Strategy pattern, and it appears throughout jygame: storage backends, audio backends, renderers, collision strategies.</p><h2 id="the-meta-principle" tabindex="-1">The Meta-Principle <a class="header-anchor" href="#the-meta-principle" aria-label="Permalink to &quot;The Meta-Principle&quot;">​</a></h2><p>Every principle above serves one goal: <strong>the architecture should make the right thing easy and the wrong thing hard.</strong></p><p>It should be easy to add a new modifier. It should be hard to accidentally allocate memory on the hot path. It should be easy to switch from CPU to GPU simulation. It should be hard to couple rendering logic to particle data.</p><p>When you see a design decision in jygame that seems unnecessary or overly complex, ask yourself: what wrong thing is this preventing?</p><p>The answer to that question is where real understanding begins.</p>',44)])])}const u=a(o,[["render",n]]);export{m as __pageData,u as default};
